let size = ['S', 'M', 'L', 'XL'];
console.log(size);
// delete the last element from the end of the array 
size.pop();
console.log(size);
// tells us that which element is removed from the array
let removed_element = size.pop();
console.log(size);
console.log(removed_element);