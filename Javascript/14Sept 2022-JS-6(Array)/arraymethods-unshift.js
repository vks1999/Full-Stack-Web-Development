let arr = [20, 30, 44];
console.log(arr);
// inserts the given element in the array from the begining
arr.unshift(5);
console.log(arr);
// tells us about the updated length of the array after inserting the elements
let len = arr.unshift();
console.log(len);