// How to create arrays in javascript

// Group of elements as one entity; where duplicates elements are allowed & heterogeneous

let a = []; // Empty array

let b = [12, 33, 4, 6, 89]; // Array of same elements or homogeneous element.
// Index 0   1   2  3   4

let c = [10, 'Twenty', true, false, null, undefined, [], {}]; // Array of heterogeneous element
// Index 0      1       2      3      4       5      6    7

console.log(c);